---
export interface Props {
	title: string;
 
}

const { title} = Astro.props;
---

<header class="py-16 text-center text-4xl">
    <h1>{title || "ui title"}</h1>
    <button id="fucker" class="text-sm bg-rose-500 text-white  font-semibold rounded-md py-2 px-4">click on me when your not strong</button>
</header>

<script>
  const fucker = document.querySelector('#fucker')
  fucker.addEventListener('click', (e)=>{
    console.log("fucker to true")
  })
</script>